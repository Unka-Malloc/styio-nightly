#pragma once

#include <string>
#include <vector>

// Number of distinct substrings via Suffix Automaton.
int
test_sam_distinct_substrings_cpp(const std::vector<int>& s);

std::string
test_sam_distinct_substrings_cpp_output(const std::vector<int>& s);
