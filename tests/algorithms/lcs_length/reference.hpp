#pragma once

#include <string>
#include <vector>

// CLRS 15.4 LCS: length of a longest common subsequence of A and B.
int
test_lcs_length_cpp(const std::vector<int>& a, const std::vector<int>& b);

std::string
test_lcs_length_cpp_output(const std::vector<int>& a, const std::vector<int>& b);
